# MySQL (Docker + optional local tools)
MYSQL_ROOT_PASSWORD=change-me
MYSQL_DATABASE=ecoshare

# App → database (Docker Compose: host is the db service name)
DB_CONTAINER=db
DB_PORT=3306
MYSQL_ROOT_USER=root
